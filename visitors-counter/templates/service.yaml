apiVersion: v1
kind: Service
metadata:
  name: {{ include "visitors-counter.fullname" . }}
  labels:
    {{- include "visitors-counter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "visitors-counter.selectorLabels" . | nindent 4 }}
