apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "visitors-counter.fullname" . }}-test-connection"
  labels:
    {{- include "visitors-counter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "visitors-counter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
